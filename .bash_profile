# ~/.bash_profile

# if personal .bashrc file exists, load its settings
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# put statements you do not repeated in sub-shells here.
# Example: update to existing PATH
# PATH=$PATH:~/bin
